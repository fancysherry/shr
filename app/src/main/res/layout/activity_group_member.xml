<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/pure_white"
    android:fitsSystemWindows="true"
    tools:context="unique.fancysherry.shr.ui.activity.GroupMemberActivity">

    <include
        android:id="@+id/group_member_activity_toolbar"
        layout="@layout/include_toolbar" />

    <LinearLayout
        android:id="@+id/group_member"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_below="@id/group_member_activity_toolbar"
        android:layout_marginTop="10dp"
        android:background="@color/pure_white"
        android:gravity="center_vertical"
        android:orientation="horizontal">


        <TextView
            android:id="@+id/group_member_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="30dp"
            android:text="@string/group_member"
            android:textColor="#c2c2c2"
            android:textSize="13sp" />
    </LinearLayout>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/group_member_activity_group_member_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/group_member"
        android:layout_marginTop="1dp"
        android:background="@color/pure_white"
        android:paddingLeft="25dp"
        android:paddingRight="25dp" />
</RelativeLayout>
